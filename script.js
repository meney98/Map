const v=document.getElementById('viewport'),m=document.getElementById('map');let s=1,x=0,y=0,drag=false,lx=0,ly=0;function draw(){m.style.transform=`translate(${x}px,${y}px) scale(${s})`}v.addEventListener('wheel',e=>{e.preventDefault();s=Math.max(.5,Math.min(8,s*(e.deltaY<0?1.15:.87)));draw()},{passive:false});v.addEventListener('pointerdown',e=>{drag=true;lx=e.clientX;ly=e.clientY;v.setPointerCapture(e.pointerId)});v.addEventListener('pointermove',e=>{if(!drag)return;x+=e.clientX-lx;y+=e.clientY-ly;lx=e.clientX;ly=e.clientY;draw()});v.addEventListener('pointerup',()=>drag=false);document.getElementById('reset').onclick=()=>{s=1;x=y=0;draw()};document.getElementById('search').addEventListener('input',e=>{/* next stage: link names to individual SVG house blocks */});
